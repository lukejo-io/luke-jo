import { NextPage } from "next";
import styles from "../styles/Home.module.css";

const Resume: NextPage = () => {
  return (
    <main className={styles.main}>
      <h1 className={styles.title}>Luke Johnson</h1>
      <div>test</div>
    </main>
  );
};

export default Resume;
